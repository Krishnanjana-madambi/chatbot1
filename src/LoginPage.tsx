import { AuthPage } from "@nhost/react";

export default function LoginPage() {
  return (
    <div style={{ maxWidth: "400px", margin: "auto" }}>
      <h2>Login / Sign Up</h2>
      <AuthPage />
    </div>
  );
}
